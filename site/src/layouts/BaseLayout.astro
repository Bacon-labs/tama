---
import "../styles/global.css";
import PrimaryNav from "../components/PrimaryNav.astro";
import SiteFooter from "../components/SiteFooter.astro";
import WashiTexture from "../components/ornaments/WashiTexture.astro";

interface Props {
  title: string;
  description?: string;
  ogTitle?: string;
  ogImage?: string;
  bodyClass?: string;
  hideFooter?: boolean;
}

const {
  title,
  description = "Tama is the Verity toolchain for Ethereum. Write a smart contract, prove what it must do, and ship the same bytecode the proof is about.",
  ogTitle,
  ogImage,
  bodyClass = "",
  hideFooter = false,
} = Astro.props;

const fullTitle = title === "Tama" ? "Tama — verified Ethereum smart contracts" : `${title} · Tama`;
const canonical = new URL(Astro.url.pathname, Astro.site ?? "https://tama.tools").toString();
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
    <meta name="theme-color" content="#1f5f4f" />
    <meta name="color-scheme" content="light dark" />

    <title>{fullTitle}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />

    <meta property="og:title" content={ogTitle ?? fullTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonical} />
    <meta property="og:site_name" content="Tama" />
    {ogImage && <meta property="og:image" content={ogImage} />}

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={ogTitle ?? fullTitle} />
    <meta name="twitter:description" content={description} />

    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <link rel="alternate icon" href="/favicon.svg" />
    <link rel="apple-touch-icon" href="/favicon.svg" />
    <link rel="sitemap" href="/sitemap-index.xml" />
  </head>

  <body class={bodyClass}>
    <WashiTexture />
    <a class="skip" href="#main">Skip to content</a>
    <PrimaryNav />
    <main id="main" class="main">
      <slot />
    </main>
    {!hideFooter && <SiteFooter />}

    <style>
      .main {
        position: relative;
        z-index: 1;
      }
      .skip {
        position: absolute;
        top: -40px;
        left: 12px;
        background: var(--jade-900);
        color: var(--washi-bright);
        padding: 8px 12px;
        border-radius: 4px;
        text-decoration: none;
        z-index: 100;
      }
      .skip:focus { top: 12px; }
    </style>
  </body>
</html>
