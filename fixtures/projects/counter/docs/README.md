# Tama Counter Fixture

This fixture exercises a compact Verity counter with `increment`, `decrement`,
and a `view` getter.

Run from the repository root:

```sh
tama --root fixtures/projects/counter check
tama --root fixtures/projects/counter build
tama --root fixtures/projects/counter test
tama --root fixtures/projects/counter audit
```
